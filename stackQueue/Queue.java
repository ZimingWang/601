package stackQueue;

/**
 */
/*
public class Queue {
    Node first,last;
    void enqueue(E t){
        Node item = new Node(t);
        if(first == null){
            last = item;
            first = last;
        }
        else {
            last.next = item;
            last = item;
        }
    }
     dequeue(){

    }

}
*/

